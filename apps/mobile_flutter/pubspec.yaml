name: cumple_now_mobile
description: Aplicación móvil de empleos temporales de Cumple Now.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.8.0

dependencies:
  file_picker: ^13.0.0
  flutter:
    sdk: flutter
  google_sign_in: ^7.2.0
  google_sign_in_web: ^1.1.3
  http: ^1.2.2
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
